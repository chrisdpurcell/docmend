# DMR-08 Closure Disposition

This disposition updates only DMR-08 and the scale-related child findings from the 2026-07-10 comprehensive review. It does not revise DMR-09 or unrelated medium/minor themes.

## Verdict

**DMR-08 is closed.** The sequential v2.0.0 contract is implemented and NFR-001 is Complete.

The landed work removes the inert `parallel.*` configuration surface, rejects plan 1.x before operational side effects, advances plans to schema 2.0, replaces the stale opt-in scale test with a default 1,000-file full-pipeline guard, and adds exact clean-HEAD installed-wheel qualification with streamed corpus generation, external stage supervision, strict public evidence, aggregate liveness, and filesystem-aware capacity preflight.

## Accepted evidence

| Proof | Accepted artifact | Result |
| --- | --- | --- |
| 100,000-file pilot | `docs/scale-evidence/accepted/14f3118e4f57c992b9d5088b9cb4f35fb3658686-pilot-100000.json` | Passing support point; zero child swap; freezes revision-two thresholds with the 10,000-file diagnostic. |
| File-size matrix | `docs/scale-evidence/accepted/f050e0aa8e2d4cf05abae09d6834e88a74a00193-file-size.json` | 12/12 configured-maximum-derived cases through 100 MiB; zero child swap; maximum stage RSS 1,894,080,512 bytes. |
| 1,000,000-file release | `docs/scale-evidence/accepted/ae3a28677390da7c823846c32af2c84b746ae861-release-1000000.json` | Passing binding run; 25,629.225 seconds; maximum stage RSS 20,825,497,600 bytes; zero child swap. |

The release evidence SHA-256 is `c5253a874159e938768d0d7cd42e8742cc8464b0044888997cf61bfca13fb7e6`. It binds candidate `ae3a28677390da7c823846c32af2c84b746ae861`, validates all four stage artifacts, reconciles 1,000,000 scanned / 875,000 applied / 100,000 clean no-ops / 25,000 plan skips, and matches the exact 25,000 expected verify findings. The frozen 25,902,581,760-byte peak-RSS, 25,804-byte/file slope, 20% linearity, reference-environment, and 43,200-second runtime limits pass.

## Finding disposition

The DMR-08-mapped scale findings are closed by the evidence above and their regression coverage:

- Cardinality and memory: the default 1,000-file source-tree guard plus installed-wheel 100,000/1,000,000 evidence replaces the stale traced-allocation test and validates bounded-linear external RSS.
- Plan/config contract: plan schema 2.0 contains no embedded runtime configuration; every legacy plan 1.x and `[parallel]` configuration shape is rejected before gates, locks, scans, reports, or mutation.
- File-size working set: the configured 100 MiB maximum is qualified independently from corpus cardinality for UTF-8 and Windows-1252 with external preservation and tool-backup cases.
- Qualification ownership and honesty: exact clean-HEAD wheel provenance, reference identity, threshold provenance, child swap, artifact validity, conservation, expected findings, and runtime are mandatory, public, and fail closed.
- Liveness and capacity: aggregate record-boundary lifecycle events document native-call silence honestly; apply preflight aggregates physical allocation by actual filesystem with persistent-artifact and largest-staging allowances.

DMR-09 remains open and is the next release blocker. This closure makes no claim about unrelated review themes or the final v2.0.0 release readiness gate.
