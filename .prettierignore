# Byte-exact test fixtures (spec §17.2 weird-document corpus): CRLF/whitespace
# ARE the test data — formatting them corrupts the corpus (.gitattributes marks
# them -text for the same reason). Sidecar .expect.json files stay byte-stable
# with their generator's output (scripts/gen_weird_corpus.py).
tests/fixtures/weird_documents/
