# Project Status

## Current snapshot

- docmend v1.0.2 is released with the complete scan, plan, apply, restore, resume, and verify pipeline.
- The approved SPEC-VHHB revision 0.28 and ADRs 0001-0021 govern post-v1 changes (adr-0006 superseded by adr-0019); the decision backlog is empty.
- The current `dev` baseline is 891 tests plus an opt-in 100k-file scale test, with 95% coverage.
- The repository workflow is `dev` to pull request to protected `main`; releases are signed `vX.Y.Z` tags with sdist and wheel artifacts. `main` stays at v1.0.2 until the v2.0.0 safety-core release ships all four remediation plans.
- The 2026-07-10 comprehensive review confirmed rollout-blocking defects (DMR-01..09); the safety core is recontracted in spec revs 0.26-0.28 with ADRs 0019-0021 from a six-round-reviewed design, targeting v2.0.0 with a clean manifest/backup format break.
- Safety-core Plan A (DMR-01/02: output ledger, write-once run/action/role backup keys, artifact destination guard with the `.docmend/` carve-out, randomized `O_EXCL` staging, in-lock report finalization) is implemented on `dev` — final whole-branch review "ready to merge", full gate green.
- Safety-core Plan B (DMR-03/04: manifest 2.0 clean break — header/chain/attempt lineage, journal-every-mutation with durable object identities, validated ManifestSet/chain with the full F5 backup trust boundary, one lifecycle reducer + crash-state adjudication shared by resume and restore, report 2.0 partition/lineage, convergent journaled restore, timeout exits) is implemented on `dev` — plan three-round Codex-reviewed before implementation; all 13 tasks landed as green slices, full gate at 97% branch coverage per commit; the opt-in scale test's manifest-shape assertion is updated (DMR-08 escrow — the scale contract itself remains sub-project 2).
- The sweep findings were independently re-verified post-Plan-A against HEAD ([verification](codex-reviews/2026-07-10-2251-all-findings-verification.md)); Plans A-C have since closed DMR-01..04 and DMR-06/07. Plan D owns DMR-05, while DMR-08/09 remain in the post-A-D sub-project queue.
- Safety-core Plan C (commit-boundary descriptor identity + attested `WriteSafetyContext`, adr-0020, DMR-06/07 + F8) is complete and locally merged into `dev`: all 12 tasks in the owner-approved [plan](superpowers/plans/2026-07-11-safety-core-c-commit-boundary.md) are committed (`571015c..ca59cdd`). Apply, restore, and adjudication now commit through descriptor-bound identity checks; overwrite preservation is enforced at action time; mutation entrypoints require factory-attested capabilities; preview paths are structurally read-only. Full gate: 891 passed, one opt-in scale skip, 95% coverage; spec rev 0.28 synchronizes §17.3 evidence for landed Plans A-C.
- Verifying a Plan C review finding exposed and fixed a live Plan B defect (`8c2d5f4`): the chain validator rejected the producer's own pre-mutation standalone `failed` records, which made any run containing an ordinary staging/backup failure unresumable and unrestorable; regression-tested at both the chain and end-to-end resume levels.
- The owner's staged real-library write rollout stays paused behind the remediation (spec section 18.4 precondition).
- Agent Handoff v1 provides one shared repo-local SessionStart runtime for the dual Claude/Codex profile.
