# Deployed

Not applicable — docmend is a local, single-user CLI with no deployment target and no environment matrix (spec §18.1/§18.3): manual `uv tool install`/`uvx` from the repo or a tagged release, no service to run, no dev/staging/prod split. Nothing to record here until that changes.
