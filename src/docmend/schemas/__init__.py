"""Checked-in artifact JSON Schemas (adr-0005) — data-only package.

A real (non-namespace) package so ``importlib.resources.files("docmend.schemas")``
resolves identically from a source checkout and an installed wheel. See README.md
in this directory for the contract and versioning policy.
"""
