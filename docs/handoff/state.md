# Handoff State

## Current focus

- Safety-core remediation per the approved design (`docs/superpowers/specs/2026-07-10-safety-core-remediation-design.md`, spec rev 0.26/0.27, ADRs 0019-0021): Plans A (DMR-01/02) and B (manifest 2.0, DMR-03/04) are implemented and pushed on `dev`. **Plan C (commit boundary, adr-0020, DMR-06/07 + F8) is AUTHORED and four owner-run Codex review rounds deep** (`docs/superpowers/plans/2026-07-11-safety-core-c-commit-boundary.md`, at `6c30f45`): all 17 findings from rounds 1-4 resolved, the CR-006 `effective_excludes` wire-model addition owner-approved 2026-07-11 (Task 8 checkpoint satisfied in-plan). NEXT: owner review round 5; on APPROVE, implement the 12 tasks (executing-plans or SDD; commit each green slice; the workflow precedent is Plan B), then Plan D (verify redesign, DMR-05) consuming Plan B's reducer/chain/`not-attempted` partition.
- Plan C rules a successor must not re-litigate (settled across rounds 1-4, each verified): terminal-honesty (a `failed` terminal only on PROVEN pre-action state; unprovable intermediates leave the intent DANGLING for adjudication) and the last-copy rule (no rollback removes a name holding the bound original unless its other name is provably intact); replacement writes stage first (`guarded_replace`, survivor verified inside the primitive at publish time); the apply capability CARRIES deep copies of the gated plan/config (`execute_plan` has no plan/config params — symmetric with restore's `safety.chain`); immutability reaches the leaves (`ObjectIdentity`/`PriorAttempt`/`ErrorInfo` frozen; tuple containers); adjudication finishes go through the same boundary; restore's carve-out excludes come from the chain root header's RECORDED `effective_excludes` (per-invocation `--exclude` flags REPLACE the config set — no restore-time reconstruction is honest).
- Landed on `dev` this session besides the plan: `8c2d5f4` fixes a live Plan B defect — the chain validator rejected the producer's own pre-mutation standalone `failed` records, making any run with an ordinary staging/backup failure unresumable/unrestorable (regressions in `tests/test_manifest_chain.py` + `tests/test_resume.py`; design lifecycle sentence disambiguated). Gate: 798 passed, 97% branch coverage.
- Plan B facts still current: manifest 2.0 is a CLEAN BREAK (no 1.x read path); shared wire types in dependency-neutral `docmend/lineage.py` (docstring is a binding import contract); the crash-state adjudication table lives in `writer/adjudicate.py`, consumed by resume and restore; identity capture stays `os.stat`-based until Plan C lands.
- The real-library write rollout stays paused behind the remediation (spec §18.4 precondition); `main` stays at v1.0.2 until the v2.0.0 release PR carries all four plans plus sub-projects 2-4.
- Keep post-v1 behavior change-controlled through the approved spec and ADR process; run the local gate before pushing `dev`. Plan docs must pass markdownlint + prettier (the reviewer checks); plan revisions are full self-contained rewrites, never references to superseded text.

## Active incidents

- None.
